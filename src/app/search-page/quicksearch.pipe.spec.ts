import { QuicksearchPipe } from './quicksearch.pipe';

describe('QuicksearchPipe', () => {
  it('create an instance', () => {
    const pipe = new QuicksearchPipe();
    expect(pipe).toBeTruthy();
  });
});
